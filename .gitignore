# Dependencies & build
node_modules/
dist/

# Electron Forge / desktop packaging outputs (large, generated by npm run package / make-portable)
out/
qvac/
forge-make.log
eztest.cjs

# Raw Playwright video (the committed mp4 in assets/social/shots/ is the deliverable)
assets/social/shots/video/
pkg-server.log
qa-pkg.log

# Downloaded model weights (large binaries; fetched at runtime by QVAC SDK)
models/

# Dev/scratch logs (the OFFICIAL demo-run audit logs live in artifacts/ and ARE committed)
logs/
*.tmp.log

# Local env & OS noise
.env
.DS_Store

# Local tool config
.claude/

# Local TLS cert (self-signed, regenerate per machine)
certs/

# Fine-tune training intermediates (adapters are kept, checkpoints are not)
artifacts/finetune/checkpoints/
artifacts/finetune-genesis/checkpoints/
data/genesis-sample.txt
data/records/
-w
